declare module 'astro:assets' {
  // Minimal declarations for the Image component. Adjust to more specific props if needed.
  // Using `any` keeps this lightweight and prevents TypeScript errors where full typings aren't available.
  export const Image: any
  export default Image
}
